// Columns in RESPONSE that are never treated as a task when computing Daily Total. Must be
// "Daily Totals" (plural) - that's this sheet's actual header text, confirmed live.
export const FIXED_RESPONSE_COLUMNS = ['Agent Name', 'Agent Email', 'Date', 'Daily Totals'];
